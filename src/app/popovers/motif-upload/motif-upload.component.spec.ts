import { ComponentFixture, TestBed, waitForAsync } from '@angular/core/testing';
import { IonicModule } from '@ionic/angular';

import { MotifUploadComponent } from './motif-upload.component';

// @ts-ignore
describe('MotifUploadComponent', () => {
  let component: MotifUploadComponent;
  let fixture: ComponentFixture<MotifUploadComponent>;

  // beforeEach(waitForAsync(() => {
  //   TestBed.configureTestingModule({
  //     declarations: [ MotifUploadComponent ],
  //     imports: [IonicModule.forRoot()]
  //   }).compileComponents();
  //
  //   fixture = TestBed.createComponent(MotifUploadComponent);
  //   component = fixture.componentInstance;
  //   fixture.detectChanges();
  // }));
  //
  // it('should create', () => {
  //   expect(component).toBeTruthy();
  // });
});
